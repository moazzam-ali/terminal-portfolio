import { Terminal } from "../src/components/Terminal";

export default function Home() {
    return (
        <main className="h-screen bg-blue-100 flex items-center justify-center p-4">
            <Terminal />
        </main>
    );
}
